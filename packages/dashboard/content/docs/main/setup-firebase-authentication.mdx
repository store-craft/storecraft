# Authentication

Setting up **Firebase** for `shelf-cms` is very simple. <br/>
`shelf` use **authentication** mainly to authenticate the admin<br/>

## Activate `Authentication` and `Admin` user

<div className='flex flex-row flex-wrap --bg-red-400 gap-5 mt-10'>
  {
    [
      { url: 'fire-5.webp', label: 'Click on Build > Authentication' },
      { url: 'fire-6.webp', label: 'Activate Auth. Click on Get Started' },
      { url: 'fire-7.webp', label: 'Authentication > Sign In Methods > Email/Password' },
      { url: 'fire-8.webp', label: 'Enable > Save' },
      { url: 'fire-9.webp', label: 'Authentication > Users' },
      { url: 'fire-10.webp', label: 'Click on Add User' },
      { url: 'fire-11.webp', label: 'Add admin user' },
      { url: 'fire-12.webp', label: 'Copy UID' },
    ].map(
      (it, ix) => (
        <Image key={ix} 
               src={`/docs/main/${it.url}`} className='w-full --sm:h-[400px] sm:w-[400px]' 
               label={`${++ix}. ${it.label}`}/>


      )
    )
  }
</div>

