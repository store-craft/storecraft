# Google Functions

Coming soon, but, it should be sufficient to use the `@storecraft/platforms/node`
package, since **Google Functions** are using **Node.js** and they
stream the usual 
- `http.IncomingMessage`
- `http.ServerResponse`