# Project

Setting up **Firebase** for `shelf-cms` is very simple. 
>You can do it manually or automatically with [firebase CLI tools](https://github.com/firebase/firebase-tools)

## Create `firebase` project and a Web App
First start by going to [firebase console](https://console.firebase.google.com/) 

<div className='flex flex-row flex-wrap --bg-red-400 gap-5 mt-10'>
  {
    [
      { url: 'fire-1.webp', label: 'Create Firebase project' },
      { url: 'fire-2.webp', label: 'Create Web App' },
      { url: 'fire-3.webp', label: 'Register App' },
      { url: 'fire-4.webp', label: 'copy projectId and apiKey' },
    ].map(
      (it, ix) => (
        <Image key={ix} 
               src={`/docs/main/${it.url}`} className='w-full sm:w-[400px]' 
               label={`${++ix}. ${it.label}`}/>
      )
    )
  }
</div>

